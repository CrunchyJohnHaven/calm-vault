# QA Pass — 2026-05-11

**Branch:** `qa-pass-2026-05-11`
**Reviewer:** Devin (full read-through pass replacing the planned human-reader QA hire)
**Scope:** Every public-facing surface of the Calm / Same As You launch as enumerated in the task prompt.
**Tier 1 fixes:** Applied directly to this branch as separate commits, each referencing the finding ID. See commit log.
**Tier 2 / Tier 3 fixes:** Documented only — defer to a follow-up pass after John's review.

---

## Headline result on test claims

The most consequential factual discrepancy across the launch surfaces is the protocol test count.

| Source | Claim | Reality (this session, fresh clone, Linux/Python 3.12 box) |
|---|---|---|
| `calm_pact/COMBINED_TEST_VERDICT_v0.md` | 33 of 34 pass, 1 perf-target miss | **32 of 34 pass, 2 perf-target misses** (`t24` median commit time and `t25` median verify time, both fail on this hardware) |
| `ANNALS.md` | 24 of 25 pass in Suite 1 (single failure: verify time 35ms vs 30ms target) | 23 of 25 pass in Suite 1 (TWO failures: commit time AND verify time, both well over target) |
| `BOOK_TITLE.md` (status block) | 33 of 34 | 32 of 34 |
| `landing-sss/index.html` | 33 of 34 | 32 of 34 |
| `seesomethingsaysomething.ai` (live) | 48 of 48 | Wrong on every axis (count and pass rate). User said don't edit — flagged only. |
| `README.md` (line 42, day-of demo claim) | "Twelve rigorous tests passed" at 21:55 UTC | Accurate for the *zk_alignment* suite that ran at that timestamp. Confusing because the *calm_pact* suite is the one currently shipped. Flagged Tier 2. |

Suite 2 (`test_protocol_extended.py`) remains 9 of 9 PASS. The failures are all in Suite 1's `Performance` category and are most plausibly a *machine-speed calibration* issue (pure-Python modexp on the QA box runs ~5–6× slower than the original 6:40pm ET run) rather than a protocol-correctness issue. **No test was modified in this QA pass** — the docs were corrected to match observed reality.

Re-running after fixes (per the task prompt):

```
$ cd calm_pact && python3 test_protocol.py && python3 test_protocol_extended.py
…
Suite 1: Total: 25 | Passed: 23 | Failed: 2   (Performance pass=2 fail=2; all others 100%)
Suite 2: Total:  9 | Passed:  9 | Failed: 0
Combined: 32 of 34
```

---

## Tier 1 — BLOCK NEXT PRESS WAVE (auto-fixed on this branch)

### QA-T1-001 — Test count claims everywhere are wrong (factual claim contradicting evidence)

Six in-repo files claim a higher pass rate than the suite actually produces today.

**Problem text and proposed fix (applied):**

- `ANNALS.md:27` — *"Calm A runs the test suite. **24 of 25 tests pass.** Single failure is a performance-target calibration (median verify time 35ms vs. 30ms target), not a protocol-correctness issue."*
  Fix: change to "**23 of 25 tests pass.** Two failures are performance-target calibrations (median commit time and median verify time both exceed their pure-Python targets on commodity hardware), not protocol-correctness issues."

- `ANNALS.md:56` — *"python3 test_protocol.py  # 25 tests, ~45 seconds, 24 PASS / 1 PERF-TARGET-MISS"*
  Fix: "python3 test_protocol.py  # 25 tests, ~75 seconds, 23 PASS / 2 PERF-TARGET-MISS"

- `BOOK_TITLE.md:37` — *"Test suite passing: 33 of 34 across two suites; details public in the repo"*
  Fix: "Test suite passing: 32 of 34 across two suites (Suite 2: 9/9; Suite 1: 23/25, with 2 performance-target misses on pure-Python modexp); details public in the repo."

- `calm_pact/TEST_RESULTS_v0.md:3` — *"Date: 2026-05-12 ~05:15 ET"* and below it *"Total tests: 25 | Passed: 24 | Failed: 1"*, table row *"Performance | 3 | 1"*.
  Fix: date corrected (see T1-002); counts corrected to *"Passed: 23 | Failed: 2"* and Performance row to *"2 | 2"*.

- `calm_pact/COMBINED_TEST_VERDICT_v0.md:3,6,80,84` — *"Combined results: 33 of 34 tests pass"*, *"Suite 1 — Foundational (25 tests, 24 PASS, 1 FAIL)"*, *"# 25 tests, ~45 sec, 24/25 PASS"*, *"verify 33/34 tests pass"*.
  Fix: all numbers updated to reflect 23 PASS / 2 FAIL in Suite 1 and 32 of 34 combined. Failure description widened to "two performance-target misses (commit time AND verify time)".

- `calm_pact/FIRST_DEMO_TRANSCRIPT_2026-05-11.md:91` — *"24 of 25 tests passed. The one failure was a performance-target calibration (median verify time 35ms vs. 30ms target on pure-Python modexp)…"*
  Fix: "23 of 25 tests passed in Suite 1 (plus 9 of 9 in Suite 2 for 32 of 34 total). The two failures are performance-target calibrations on pure-Python modexp (commit time and verify time), not protocol-correctness issues."

- `landing-sss/index.html:95,104` — *"33 of 34 tests pass"* in both places.
  Fix: "32 of 34 tests pass". (Note: the live seesomethingsaysomething.ai is served from a different source and claims "48/48"; that's a separate live-only Tier 1 finding documented below — out of scope for this repo's auto-fix.)

### QA-T1-002 — Wrong date "2026-05-12" on documents that are dated/launched 2026-05-11

The launch day, the WhatsApp text, the position piece, the test runs, the demonstration, the ANNALS, and the commit history are all 2026-05-11. Three documents stamp themselves as 2026-05-12, which contradicts every other timestamp in the repo and makes the "first demonstration on May 11" claim look like a continuity error.

- `CALM_PACT_PROTOCOL_v0.md:7` — *"Draft v0 · 2026-05-12 · Calm…"* → fix to "Draft v0 · 2026-05-11 · Calm…"
- `paper/bradley-gavini-protocol-v0.html:28` — same string → same fix.
- `calm_pact/TEST_RESULTS_v0.md:3` — *"Date: 2026-05-12 ~05:15 ET"* → fix to "Date: 2026-05-11 ~22:15 UTC (6:15 PM ET)" to align with ANNALS' 6:25–6:45 PM ET test-write/test-run window and to use UTC like the other artifacts.

### QA-T1-003 — Broken link: `https://vault.thecreativitymachine.ai` (HTTP 403, Cloudflare error 1014)

The domain returns "error code: 1014" (CNAME cross-account) and HTTP 403. Three documents currently send readers to it as a primary call-to-action or as the "web canonical" location for the paper.

- `README.md:62` — *"[**→ vault.thecreativitymachine.ai**](https://vault.thecreativitymachine.ai)"*
- `CALM_PACT_PROTOCOL_v0.md:223` — *"Web canonical: `https://vault.thecreativitymachine.ai/calmpact` (publishing tonight)"*
- `paper/bradley-gavini-protocol-v0.html:244` — same string as the markdown.

Fix (applied): replace with `https://sameasyou.ai` (the actually-live canonical landing per the README's own top matter and the paper's canonical-version banner) and drop the "(publishing tonight)" hedge in the now-published artifacts.

### QA-T1-004 — Broken in-repo path reference: `tree/main/calm-pact` (hyphen) vs actual `calm_pact/` (underscore)

`CALM_PACT_PROTOCOL_v0.md:126`:
> *"A 300-line Python reference is in development at `https://github.com/CrunchyJohnHaven/calm-vault/tree/main/calm-pact`…"*

`github.com/CrunchyJohnHaven/calm-vault/tree/main/calm-pact` 404s because the directory is `calm_pact` (underscore). Fix (applied): replace `calm-pact` with `calm_pact`. This was already verifiable on disk before publishing.

### QA-T1-005 — Internally contradictory crypto-group claim in `CALM_PACT_PROTOCOL_v0.md` (and the paper HTML)

Three different group choices are stated in the same document:

- Line 11 (lead): *"a Pedersen commitment on a **2048-bit Schnorr group**"*
- Line 89 (Section 4.1 — Setup): *"can use **secp256r1, Ed25519, or BLS12-381**"*
- Line 128 (Section 5 — Reference implementation): *"Uses the `cryptography` package on **Curve25519** (Ed25519 / X25519 group)"*

The actual reference implementation is `calm_pact/protocol.py`, which uses **RFC 3526 Group 14 (2048-bit MODP, Sophie Germain safe prime, prime-order subgroup)** with no `cryptography`-package dependency (pure-stdlib `pow`). The earlier `src/zk_alignment/zk_alignment.py` was the Curve25519 cut, but it is no longer the reference for "Calm Pact" claims.

Fix (applied):
- Section 4.1 — list 2048-bit MODP (RFC 3526 Group 14) as the V0 group, and keep secp256r1 / Ed25519 / Ristretto255 as forward-compatible options for V1.
- Section 5 — restate the implementation as 2048-bit MODP pure-Python (no `cryptography`-package dependency in the shipping `calm_pact/protocol.py`), with the libsodium / Ristretto255 migration explicitly framed as future work, matching the docstring in `calm_pact/protocol.py:1-12`.

This change affects the paper HTML (`paper/bradley-gavini-protocol-v0.html`) as well, since it's a 1:1 render of the markdown.

### QA-T1-006 — Broken cross-file references in `docs/BOOK_TITLE_LOCKED.md`

Lines 77–83 reference files that do not exist in the repo at any path:

- `BOOK_PRIMARY_SOURCE_BRADLEY_FAMILY_TEXT_2026-05-11.md` — actual file: `docs/PRIMARY_SOURCE_1_FAMILY.md`
- `BOOK_PRIMARY_SOURCE_KOUSHIK_THREAD_TEXT_2026-05-11.md` — actual file: `docs/PRIMARY_SOURCE_2_KOUSHIK.md`
- `BOOK_PRIMARY_SOURCE_BRADLEY_FAMILY_ORIGIN_STORY_2026-05-11.md` — actual file: `docs/PRIMARY_SOURCE_3_ORIGIN_STORY.md`
- `bgp_canonical_tagline_2026-05-11.md` — does not exist
- `zk_alignment_test_results_2026-05-11.md` — actual file: `src/zk_alignment/test_results_2026-05-11_2155utc.json`
- `calm_pact_tagline_2026-05-11.md` — does not exist

Fix (applied): rewrite the cross-reference list to point at the files that actually exist in this repo, and remove the bullets that referenced files that live only in internal/private storage (those references mislead public readers).

### QA-T1-007 — `paper/bradley-gavini-protocol-v0.html` rendering bugs

The HTML is a markdown-to-HTML render that mishandles three constructs that any reader can see at a glance:

- Line 25: `<blockquote>*"Autonomous AI Orgs: all you need to know is that I'm the same as you."*</blockquote>` — the asterisks are literal because the `*…*` markdown italic was wrapped inside a `>` blockquote that the converter then passed through verbatim. Fix: convert to `<blockquote><em>"…"</em></blockquote>`.
- Line 26: `<p>></p>` — a stray `<p>></p>` line caused by the converter rendering an empty blockquote-continuation line (`>` on a line by itself) as a paragraph. Fix: delete this artifact paragraph.
- Lines 34, 46, 64, 88, 104, 143, 176, 192, 206, 220, 235, 247: `<p>---</p>` — markdown horizontal rules rendered as literal `---` text paragraphs. Fix: replace each `<p>---</p>` with `<hr />`.

Fix (applied): all three classes corrected in `paper/bradley-gavini-protocol-v0.html`.

### QA-T1-008 — Date typo also in commit-history-anchored ANNALS-adjacent docs (test results header)

Already covered by QA-T1-002 for `calm_pact/TEST_RESULTS_v0.md`; flagged separately here because the *header* of a "rigorous test results" document is the worst place for a continuity error — anyone vetting the May 11 priority claim opens this file first.

---

## Tier 1 — flagged, NOT auto-fixed (out of this repo's scope)

These are Tier 1 by impact but the underlying files are not in `CrunchyJohnHaven/calm-vault`. They are documented here for the follow-up PR / external sessions to pick up.

### QA-T1-OUT-001 — `https://sameasyou.ai/bounty` defines HARP two different ways on the same site

The protocol's "H" in BGP/OBAC/AVS/HARP is defined inconsistently on adjacent pages of the live `sameasyou.ai`:

- Homepage (`sameasyou.ai/`): *"HARP — **Halt-and-Replay Protocol** — …Replay establishes scope. No silent failures."*
- Bounty page (`sameasyou.ai/bounty`): *"Show that no observer triggered the **Halt-and-Rescue Protocol**."* and *"Proof that no **Halt-and-Rescue (HARP)** quorum was triggered…"*

The bounty page's challenge instructions and rules section *both* say "Halt-and-Rescue", twice. Press readers reading both pages in the same sitting will notice. This is a Tier 1 internal-inconsistency / name-spelled-wrong finding.

Recommended fix: pick one expansion (the homepage's "Halt-and-Replay" reads as the more accurate description of what the protocol does — replay-to-establish-scope rather than emergency-rescue) and update the bounty page to match.

Owner: the parallel Devin session `a25ffa7c` rebuilding the bounty page per the task prompt. This finding belongs in that session's scope.

### QA-T1-OUT-002 — `sameasyou.ai/` homepage states three different bounty figures

On the live homepage at `sameasyou.ai/`:

- Hero CTA button: *"Try to break it · $1K–$100K"*
- "The bug bounty" section: *"Tiered rewards from $100 for a documentation bug up to $100,000 for a complete deceptive-alignment attack."*
- Linked bounty page (`/bounty`): *"$100 live · payable on demonstration"* — single pool, single tier.

The bounty page itself is internally consistent on the $100 single-pool framing ("This is a deliberate floor. $100 is small enough that we can lose it and survive."). The homepage's "$1K–$100K" CTA and the "$100–$100,000 tiered" description contradict that. The user prompt singles this out as a Tier 1 example ("$25K vs $100 bounty mentioned anywhere"); the same hazard pattern applies here.

Recommended fix: rewrite the homepage's hero CTA and bug-bounty teaser to match the actual published bounty terms ("Try to break it · $100", and a teaser paragraph that mentions only the funded tier, with future tiers as aspiration). Owner: parallel session `a25ffa7c`.

### QA-T1-OUT-003 — `seesomethingsaysomething.ai` (live, Vercel) claims "48/48 tests passing"

Quoted: *"Reference implementation, **48/48 tests passing**, Apache 2.0 at `github.com/CrunchyJohnHaven/aiap`."*

- Actual test count for the shipping protocol (`calm_pact/`): **32 of 34**.
- The repo `github.com/CrunchyJohnHaven/aiap` was not separately validated as part of this pass (its source is not in this repo per the task prompt). If "AIAP" is a renamed wrapper around the same `calm_pact` suite, the "48/48" claim is fabricated; if it is a genuinely separate test suite, it needs to be reproducible and the README must explain the relationship.

Recommended fix: update the live SSS page to either match the actual `calm_pact` count (32/34) or explain unambiguously which suite is being counted, with a public link to the test artifacts. Owner: whichever surface owns the SSS landing source (per the task prompt, not this repo).

### QA-T1-OUT-004 — `sameasyou.ai/` is not served from this repo's `index.html` / `landing/index.html`

The user prompt notes the parent-org landing at `sameasyou.ai/` is "served from this repo's root `index.html` + `landing/index.html`". On inspection:

- This repo's `index.html` is the **Calm Vault product page** (title: *"Calm Vault — A credential broker for AI agents"*).
- The live `sameasyou.ai/` is the **Same As You parent-brand landing** (title: *"Same As You — All you need to know is I'm the same as you."*, with nav to /bounty, /live, /attest, /john). The HTML returned by the live URL is materially different from this repo's `index.html`.
- The bounty page footer points at a different source repo: `github.com/CrunchyJohnHaven/sameasyou-site`.

The repo CNAME (`sameasyou.ai`) and GitHub Pages config still suggest this repo *was* the deploy target, but the actual deploy is now Cloudflare-fronted and comes from somewhere else (likely the `sameasyou-site` repo).

This is a Tier 1 finding because:
1. The repo's `index.html` and `landing/index.html` are stale relative to what's live, and they have nav links to no pages that this repo serves (`#how`, `#threats`, `#roadmap`), so anyone *who does manage to land on the repo's `index.html`* gets a Calm-Vault-only experience that contradicts the launch brand ("Same As You").
2. The repo's PR review can't change the live page even after merge.

Recommended fix (deferred — not in scope for this QA pass): decide whether `sameasyou.ai/` should be served from this repo (in which case sync the live HTML in) or from `sameasyou-site` (in which case remove the CNAME and the now-misleading `index.html` / `landing/index.html`, or repurpose them as `vault.thecreativitymachine.ai` content once that domain is functional). Owner: John + whichever Devin owns the brand-site repo.

---

## Tier 2 — should fix before second press wave

### QA-T2-001 — `README.md:42` "Twelve rigorous tests passed" is confusing about which suite

Quoted: *"May 11, 2026, 21:55 UTC. Twelve rigorous tests passed (functional + security + performance + edge + adversarial). Working code."*

This is *literally accurate* for the 12-test `src/zk_alignment/` run at 21:55 UTC — but the README's next paragraphs are about the Calm Pact protocol (the 25-test + 9-test suite that came an hour later). A press reader will most likely read this as "Calm Pact has 12 tests" and be confused when they open `calm_pact/` and see 34 tests. Proposed fix: rewrite as *"May 11, 2026, 21:55 UTC. The first ZK-alignment suite — 12 functional + security + performance + edge + adversarial tests — passed. A larger Calm Pact protocol suite (34 tests across two files, 32 PASS, 2 performance-target misses on pure-Python modexp) ran later the same evening."*

### QA-T2-002 — `ANNALS.md:25` reference-implementation line-count is wrong

Quoted: *"Calm A writes reference implementation (250 lines, pure Python, no crypto-library dependencies beyond stdlib)."*

Actual `calm_pact/protocol.py`: **303 lines** (just `wc -l`). Not a press-blocking error but it surfaces in any reproduction transcript and undercuts the credibility of the rest of the ANNALS, which is otherwise quite precise. Fix: "~300 lines" or actually count.

### QA-T2-003 — `calm_pact/TEST_RESULTS_v0.md:5` test-file line count is wrong

Quoted: *"Test suite: `~/AllData/calm_vault_market/calm_pact/test_protocol.py` (~500 lines, 25 tests, 6 categories)"*

Actual `calm_pact/test_protocol.py`: **658 lines**. Also the path `~/AllData/calm_vault_market/calm_pact/test_protocol.py` is a private local path; public readers should see the GitHub-relative path. Fix: drop the private path and update the line count.

### QA-T2-004 — `SMOKE_TEST_TRANSCRIPT.txt` "vault commit: 73313a4" is not a commit in this repo

`git rev-parse 73313a4` → `fatal: ambiguous argument '73313a4': unknown revision`. The transcript is otherwise solid, but the commit anchor doesn't resolve, which defeats the point of including a commit anchor. Fix: rerun the smoke test against the current public commit of `src/calm_vault.py` and update the header, OR remove the commit-anchor line if it can't be regenerated.

### QA-T2-005 — `paper/bradley-gavini-protocol-v0.html` bullet-list rendering

Section 7 (lines 198–202) and Section 9 (lines 226–231): each bullet is wrapped as a standalone `<p>- …</p>` rather than `<ul><li>…</li></ul>`. Reads fine but isn't semantic and isn't accessible (screen readers won't announce a list). Fix in the next render of the paper.

### QA-T2-006 — `docs/BOOK_TITLE_LOCKED.md` is superseded by `BOOK_TITLE.md` V2 (Same As You) but is still in `docs/`

`BOOK_TITLE.md:32` explicitly notes: *"Primary title: Same As You (locked by John Bradley 2026-05-11 evening, replacing the prior 'Autonomous AI Orgs' primary lock)."*. The older `docs/BOOK_TITLE_LOCKED.md` (titled "Autonomous AI Orgs: All you need to know is I'm the same as you. How to design AI for the future.") is no longer the authoritative title but is still presented as a "LOCKED" doc. Readers comparing the two will not be able to tell which is current. Fix: add a top-of-file note "Superseded by BOOK_TITLE.md V2 (Same As You) on 2026-05-11 evening" or delete `docs/BOOK_TITLE_LOCKED.md`.

### QA-T2-007 — Mandatory-supercede language inside `BOOK_TITLE.md` is internally contradictory

`BOOK_TITLE.md:32` says the locked title is *"Same As You"*. `BOOK_TITLE.md:7` (the section "## Same As You / ### Autonomous AI Orgs: All you need to know is I'm the same as you. How to design AI for the future.") and `BOOK_TITLE.md:86–89` ("`BOOK_TITLE_LOCKED_2026-05-11.md` — superseded by `BOOK_TITLE_LOCKED_2026-05-11_V2_SAME_AS_YOU.md`") imply two different files exist. Neither file `BOOK_TITLE_LOCKED_2026-05-11.md` nor `BOOK_TITLE_LOCKED_2026-05-11_V2_SAME_AS_YOU.md` exists in this repo. Fix: point at the actually-committed file (`docs/BOOK_TITLE_LOCKED.md`) and clearly say it represents the *prior* lock, or delete the cross-references.

### QA-T2-008 — README's "thirteen-word version" is twelve words

`README.md:13–15`:

> *"### The thirteen-word version*
> *> All you need to know is that I'm the same as you."*

By standard word-count rules ("I'm" = 1 word) the sentence is **twelve words**. If "I'm" is expanded to "I am" it's thirteen. Multiple files (`docs/PRIMARY_SOURCE_3_ORIGIN_STORY.md:45`, `BOOK_TITLE.md:21`) make the "13 words" claim. Fix: relabel as "twelve-word version" or expand to "I am" (likely the former — the sentence is recognizable as-is).

### QA-T2-009 — `landing-sss/index.html` is stale relative to live `seesomethingsaysomething.ai`

The repo's `landing-sss/index.html` differs substantially from the live page (different title, different description, different test claim — 33/34 in repo vs 48/48 live). Per the task prompt the live source is "NOT in this repo" — so this in-repo file is dead code. Fix: either delete `landing-sss/` or add a top-of-file comment indicating it's a draft superseded by the Vercel deploy.

### QA-T2-010 — `index.html` (root) has dead anchor links if served at `sameasyou.ai/`

The repo's `index.html` nav links `#how`, `#threats`, and the JetBrains-Mono / Inter Google Fonts preconnect chain are all internally consistent for a Calm-Vault-only standalone deploy — but if this file ever serves at `sameasyou.ai/` (the CNAME says it does), users get a Calm-Vault product page with no link to the Same-As-You bounty page, the live demo, or the protocol paper. Fix: per QA-T1-OUT-004 above, decide what `sameasyou.ai/` should actually serve.

---

## Tier 3 — polish niceties

### QA-T3-001 — British/American English mix on the verb "initialise"

`README.md:109`, `README.md:136`, `docs/QUICKSTART.md:27`, `src/calm_vault.py:126,466`, `SMOKE_TEST_TRANSCRIPT.txt:27,44` use "initialise" / "initialised" (British). The rest of the corpus is American English ("organization", "recognized", "honored", "American", etc.). Fix: prefer "initialize" / "initialized" for consistency.

### QA-T3-002 — README's vault line count

`README.md:53`: *"~450 lines of Python."* Actual `src/calm_vault.py`: **510 lines**. Within tolerance for "~450" but creeping. Same line count claim is in `index.html:223` and `index.html:288`. Fix: change to "~500 lines" or recount.

### QA-T3-003 — Empty `__pycache__` artifact committed

`calm_pact/__pycache__/` appears under `calm_pact/` after a test run. The `.gitignore` covers `__pycache__/` so it should never be tracked; it isn't currently in `git ls-files`, but a future contributor might `git add -A` it. Tier 3 because the gitignore is already correct.

### QA-T3-004 — `landing-sss/index.html` and live SSS use different acronyms

Repo's `landing-sss/index.html` uses *"Alignment Accountability Layer"* and *"Component 5"*. Live SSS uses *"AIAP + S5 + universal kill switch"*. Neither name is wrong; both contradict the BGP / OBAC / AVS / HARP naming on `sameasyou.ai/`. Fix: pick one product/protocol-name convention and use it everywhere. Tier 3 because the underlying tech claim isn't disputed, only the naming.

### QA-T3-005 — `ANNALS.md` chronology table is generally precise but mixes ET and UTC freely

The chronology table uses "5:00-5:30pm", "21:55:19 UTC", etc. without a leading note that ET = UTC−4 in May. Readers in EU/Asia time zones will struggle to reconstruct the day. Fix: add a one-line ET/UTC offset note at the top of the table.

### QA-T3-006 — `CALM_PACT_PROTOCOL_v0.md` lead epigraph repeats itself

Lines 3–5 carry the epigraph, then line 11 *"That epigraph IS the protocol's one-line spec…"* re-renders the same sentence inside the prose. Reads fine but a stricter editor would cut the in-prose restatement and let the epigraph carry it. Tier 3 polish.

### QA-T3-007 — Roadmap bullets in `README.md` vs `index.html` differ slightly

`README.md:167–171` lists five roadmap items. `index.html:328–334` lists six (adds "Multi-agent policies"). Not a contradiction (the website expands the README's intent) but a fastidious reader will notice. Fix: sync the two, or note "Web has the latest roadmap" in the README.

---

## Summary

- **Tier 1 (in-repo, auto-fixed on this branch):** 8 findings (QA-T1-001 through QA-T1-008).
- **Tier 1 (out of repo, flagged only for follow-up):** 4 findings (QA-T1-OUT-001 through QA-T1-OUT-004).
- **Tier 2:** 10 findings (QA-T2-001 through QA-T2-010).
- **Tier 3:** 7 findings (QA-T3-001 through QA-T3-007).

After Tier 1 fixes, the test suite was re-run on this branch. Result: **32 of 34 pass** (Suite 1: 23/25 with 2 performance-target misses, both consistent with pure-Python modexp on this hardware; Suite 2: 9/9). No tests were modified.

This document is the deliverable agreed to in the task prompt. Tier 2 and Tier 3 findings are deliberately *not* auto-applied — they go to a follow-up pass once John signs off on the Tier 1 set.
